import {Component, OnInit} from '@angular/core';
import {FormBuilder, FormGroup} from '@angular/forms';

@Component({
  selector: 'app-baseAmortiguador',
  templateUrl: './controlArm.component.html',
  styleUrls: ['./controlArm.component.sass']
})

export class ControlArmComponent implements OnInit {

  constructor(
  ) { }

  ngOnInit() {
  }
}
