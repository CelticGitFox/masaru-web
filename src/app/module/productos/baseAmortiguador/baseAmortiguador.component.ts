import {Component, OnInit} from '@angular/core';
import {FormBuilder, FormGroup} from '@angular/forms';

@Component({
  selector: 'app-baseAmortiguador',
  templateUrl: './baseAmortiguador.component.html',
  styleUrls: ['./baseAmortiguador.component.sass']
})

export class BaseAmortiguadorComponent implements OnInit {

  constructor(
  ) { }

  ngOnInit() {
  }
}
